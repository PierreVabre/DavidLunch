
<a class="back" href="javascript:history.back()">RETOUR</a>

<div class="section section_site_title">
	<h1 class="site_title"><%= @category.title %>
		<div alt="ceci est un point gif" class="point" style="background-image: url(<%= @category.gif %>)"></div>
	</h1>

</div>

<div class="section section_categories_list">
	<% @category_subcategories.each do |subcategory| %>
		<%= render partial: "/subcategories/subcategory_card", locals: {subcategory: subcategory} %>
	<% end %>
	<div class="clearfix"></div>
</div>

<div class="section categorie_adresses_list section_content">
	<div class="tri">
		<div class="label">Trier par</div>
		<a class="unactive" href="?tri=id">Date d'ajout</a>
		<a class="unactive" href="?tri=price">Prix croissant</a>
		<a class="unactive" href="?tri=title">Ordre alphabétique</a>
		<!-- <a class="unactive" href="?tri=distance">Distance</a>
		<a class="unactive" href="?tri=like">Likes</a> -->
	</div>
	<% @category_adresses.each do |adresse| %>
		<%= render partial: "/adresses/adresse_card", locals: {adresse: adresse} %>
	<% end %>
	<div class="clearfix"></div>
</div>